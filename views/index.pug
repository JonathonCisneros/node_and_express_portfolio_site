extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
        
      h1 My Portfolio
      
      p.op.lead.text-light
        | Please enjoy my skills through the following projects 
        
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      
      each project in projects
          a.project(href=`/projects/${project.id}`)
              img.thumbnail.box-shadow(src=`${project.image_urls[0]}`)
              h5.op= project.project_name
         
      
